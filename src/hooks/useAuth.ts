// src/hooks/useAuth.ts
import { useAuth } from '../contexts/AuthContext';

/**
 * Custom hook to access authentication state and user information from AuthContext.
 * This is a re-export from the AuthContext for convenience and consistency.
 */
export { useAuth };
