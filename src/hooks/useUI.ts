// src/hooks/useUI.ts
import { useUI } from '../contexts/UIContext';

/**
 * Custom hook to access global UI functionalities (e.g., modals) from UIContext.
 * This is a re-export from the UIContext for convenience and consistency.
 */
export { useUI };
